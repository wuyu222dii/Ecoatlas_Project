/**
 * Data access layer (Spring Data JPA repositories).
 * <p>
 * Repository interfaces talk to the database; each typically maps to one table with CRUD and custom queries.
 * <p>
 * Note: some projects keep DAO/mapper elsewhere (e.g. MyBatis XML); this app uses JPA repositories here.
 */
package com.example.demo.repository;
