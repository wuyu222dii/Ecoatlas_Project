spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres
    username: postgres.mvfbbbffqgfayouayjny
    password: SdN6pBoFqW4CP7Mu
    hikari:
      # Supabase pooler (port 5432) has a low per-database connection cap (often ~15); keep pool small.
      maximum-pool-size: 3
      minimum-idle: 1
      max-lifetime: 540000
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false
  flyway:
    baseline-on-migrate: true
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=2000,expireAfterAccess=120s
  mail:
    host: smtp.gmail.com
    port: 587
    username: txin987@gmail.com
    password: "fdhm svtk wruq yfwh"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  data:
    redis:
      repositories:
        enabled: false

# JWT
jwt:
  secret: ${JWT_SECRET:SixCookiesJwtSecretKeyForAuthPleaseChangeInProduction}
  expiration-ms: 315360000000

# Logging (reduce Netty DNS noise, etc.)
logging:
  level:
    io.netty: WARN
    org.springframework.boot.autoconfigure.security: WARN

# Google OAuth2 (id_token verified by GoogleIdTokenVerifier; no tokeninfo URL)
google:
  # Must match front-end VITE_GOOGLE_CLIENT_ID (same OAuth Web client)
  client-id: 639978587854-7qescuust62osbkmu4f63unjpdg5k9re.apps.googleusercontent.com
  # Places API (New). Set GOOGLE_MAPS_API_KEY in env or deployment; do not commit real keys.
  maps:
    api-key: AIzaSyDZCtBFmlhL-2YzqYmiCVblzBQL5LHIHgI
    base-url: https://places.googleapis.com/v1
    request-timeout-seconds: 15

# Gemini (EchoAtlas AI). Use env vars in production; do not commit real keys to public repos.
ai:
  gemini:
    api-key: AIzaSyCKLNTFgWpMKVayZQgTbLpdjX8GxS4jIsc
    model: gemini-2.0-flash
    base-url: https://generativelanguage.googleapis.com/v1beta
    temperature: 0.45
    max-output-tokens: 8192
    request-timeout-seconds: 90

# Spotify Web API (user OAuth, search, tracks). Fill in after creating an app in the Spotify Dashboard.
spotify:
  client-id: c03c84cdc14a4f8099bc6b6c4a086bd0
  client-secret: bc48b90d28054243b6cb35deebea80e5
  # Must match a Redirect URI in the Dashboard (page that receives ?code=)
  default-redirect-uri: http://127.0.0.1:5173/spotify-callback
  scopes: user-read-email user-read-private user-library-read user-library-modify
  state-ttl-minutes: 10
  http-timeout-seconds: 30